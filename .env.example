# Optional overrides. Copy to .env (ignored by git) only if you need them.
# Use full paths because these values are not shell-expanded.
# INFRA_STATE_DIR=/home/youruser/.infra-state
# ITEM_PROXMOX_TOKEN=infrastructure/proxmox-api-token
# ITEM_STATE_PASSPHRASE=infrastructure/tofu-state-passphrase
