# Internal addresses only. Public IPs and real domains stay out of this repo.
all:
  children:
    managed:
      children:
        openbao_hosts:
        gitlab_hosts:
        authentik_hosts:

    openbao_hosts:
      hosts:
        openbao-1:
          ansible_host: 10.69.10.10

    gitlab_hosts:
      hosts:
        gitlab-1:
          ansible_host: 10.69.10.11

    authentik_hosts:
      hosts:
        infrastructure-stack:
          ansible_host: 10.69.10.14
