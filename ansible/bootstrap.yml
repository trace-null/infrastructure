# Bootstrap layer: OpenBao, GitLab and the runner. Always run by hand.
- name: OpenBao
  hosts: openbao_hosts
  become: true
  roles:
    - baseline
    - openbao

- name: GitLab
  hosts: gitlab_hosts
  become: true
  roles:
    - baseline
    - gitlab
